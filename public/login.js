import {$,api} from './shared.js';$('#login-form').addEventListener('submit',async e=>{e.preventDefault();const button=e.target.querySelector('button');button.disabled=true;button.textContent='Signing in…';$('#login-error').textContent='';try{await api('login',Object.fromEntries(new FormData(e.target)));location.replace('/admin');}catch(error){$('#login-error').textContent=error.message;button.disabled=false;button.textContent='Sign in securely ↗';}});
